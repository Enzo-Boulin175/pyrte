# pyrte

`pyrte` est une bibliothèque Python simple permettant d’interagir avec les API de RTE (Réseau de Transport d’Électricité).  
Elle fournit une interface haut niveau pour l’authentification et la récupération de données depuis les endpoints RTE.

## Installation

Depuis PyPI :

```bash
pip install py-rte
